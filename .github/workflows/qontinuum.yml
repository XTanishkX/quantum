name: Qontinuum (dogfood)

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  quantum-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./action
        with:
          path: examples
          seed: "42"
          install: ".[ibm]"
